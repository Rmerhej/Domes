package fr.greta92.Domes.repository;
/*
import fr.greta92.Domes.beans.Article;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;



import java.util.List;

    public interface SearchArticleRepository extends JpaRepository<Article, Integer> {

        @Query("select a,p from Article a,Produit p where a.id_produit = :id_produit and p.nom_produit = :nom_produit")
        public List<Article> searchArticleByIdNom(@Param("id_produit") Integer id_produit, @Param("nom_produit") String nom_produit);


    }*/

